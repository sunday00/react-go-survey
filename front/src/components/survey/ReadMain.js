import React from 'react';

const ReadMain = ({ title, description }) => {
  //TODO:: make buttons : go back to list / start
  return (
    <div>
      <h2>{title}</h2>
      <p>{description}</p>
    </div>
  );
};

export default ReadMain;
